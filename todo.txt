History via alpaca: https://docs.alpaca.markets/us/reference/stockbars

Prices go stale if backend server is down (scheduler doesn't run) - need to streamline
how prices get updated (on watchlist add, on position sync, on login, etc.)

Refreshing price should refresh % change

Should be able to search watchlist/positions

Thesis statements should be tagged to a price point

- watchlist/position collisions not resolving properly
- Load animation is slow now when adding to watchlist

UI Changes:
    - Focus on window focus (prevent stale state)
    - stocks already in watchlist should be marked as followed somehow in the search
    - Handle delete behaviour better when main UI changes

Edge case behaviour when snaptrade credentials are unset?

Null price behaviour handling. Should be a flyway migration handler that just fills
them with 0, and a regression test

Thesis statements should persist after removal from watchlist but maybe get marked as stale?

Add linter

Aggregation (single /dashboard API)